#include <stdio.h>
#include <conio.h>
#define f(x) x *x
int main()
{
    int x = 1, y = 1000;
    if (y > 1 || x = 0)
    {
        printf("Hello");
    }
    else
    {
        printf("World");
    }
}